# ANANSI Combined Professional SVG UI Pack v2

Updated per your requested changes:

1. The logo symbol has been normalized to the same flat brand colors as the wordmark:
   - Void Black `#050505`
   - Hunt Red `#C1121F`

2. The favicon/app icon exports have been rescaled so the full spider legs are visible and no longer cut off at the bottom.

## Contents
- `brand/symbol/` — exact symbol, recolored to flat brand colors
- `brand/wordmark/` — approved wordmark
- `brand/lockups/` — combined horizontal + stacked lockups
- `favicon/` — corrected favicon set
- `app-icon/` — app icon exports
- `social/` — social assets
- `source/` — original and recolored symbol source + wordmark source
